import React from "react"

import { NavItem } from "./NavItem"

const NavbarLinks = () => (
  <>
    <NavItem to="/about">intro</NavItem>
    <NavItem to="/portfolio">portfolio</NavItem>
    <NavItem to="/contact">biz enquiries</NavItem>
  </>
)

export default NavbarLinks
